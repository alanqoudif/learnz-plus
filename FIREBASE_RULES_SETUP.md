# إعداد قواعد Firebase - حل مشكلة الأذونات

## 🚨 المشكلة:
```
ERROR ❌ خطأ في إنشاء/تحديث المعلم: [FirebaseError: Missing or insufficient permissions.]
ERROR ❌ Firebase getClassesByTeacher error: permission-denied Missing or insufficient permissions.
```

## ✅ الحل:

### 1. إعداد قواعد Firestore Database:

1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. اختر مشروعك: `ttttt-13caf`
3. من القائمة الجانبية، اختر **"Firestore Database"**
4. اضغط على تبويب **"Rules"**
5. استبدل القواعد الموجودة بهذا الكود:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // قواعد مبسطة للاختبار - تسمح بالوصول للمستخدمين المسجلين
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

6. اضغط **"Publish"** لحفظ القواعد

### 2. إعداد قواعد Realtime Database:

1. من القائمة الجانبية، اختر **"Realtime Database"**
2. اضغط على تبويب **"Rules"**
3. استبدل القواعد الموجودة بهذا الكود:

```json
{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null"
  }
}
```

4. اضغط **"Publish"** لحفظ القواعد

### 3. تفعيل Authentication:

1. من القائمة الجانبية، اختر **"Authentication"**
2. اضغط على تبويب **"Sign-in method"**
3. فعّل **"Email/Password"**
4. اضغط **"Save"**

### 4. تفعيل Firestore Database:

1. من القائمة الجانبية، اختر **"Firestore Database"**
2. إذا لم تكن مُفعلة، اضغط **"Create database"**
3. اختر **"Start in test mode"** (للاختبار)
4. اختر موقع قاعدة البيانات (أقرب موقع لك)

### 5. تفعيل Realtime Database:

1. من القائمة الجانبية، اختر **"Realtime Database"**
2. إذا لم تكن مُفعلة، اضغط **"Create database"**
3. اختر **"Start in test mode"** (للاختبار)
4. اختر موقع قاعدة البيانات (نفس موقع Firestore)

## 🧪 اختبار الإعداد:

بعد تطبيق القواعد، جرب التطبيق مرة أخرى:

1. **تسجيل الدخول** - يجب أن يعمل بدون أخطاء
2. **إضافة فصل** - يجب أن يحفظ في Firestore
3. **إضافة طلاب** - يجب أن يحفظ في Firestore
4. **تسجيل الحضور** - يجب أن يعمل

## 📊 التحقق من البيانات:

1. اذهب إلى **Firestore Database** → **Data**
2. يجب أن ترى الكولكشنات:
   - `teachers` - بيانات المعلمين
   - `classes` - الفصول الدراسية
   - `students` - بيانات الطلاب
   - `attendance_sessions` - جلسات الحضور
   - `attendance_records` - سجلات الحضور

## 🔐 ملاحظات الأمان:

- هذه القواعد مبسطة للاختبار
- تسمح لأي مستخدم مسجل بالوصول لجميع البيانات
- يمكن تحسينها لاحقاً لتكون أكثر أماناً

## 🎯 النتيجة المتوقعة:

بعد تطبيق هذه القواعد:
- ✅ لن تظهر رسائل خطأ الأذونات
- ✅ ستعمل جميع وظائف التطبيق
- ✅ ستظهر البيانات في Firebase Console
- ✅ سيعمل التطبيق بشكل مثالي

## 🚨 إذا استمرت المشكلة:

1. تأكد من تطبيق القواعد بشكل صحيح
2. تأكد من تفعيل جميع الخدمات
3. جرب تسجيل الخروج والدخول مرة أخرى
4. تأكد من اتصال الإنترنت
