# 📱 دليل رفع التحديثات - تطبيق الحضور والغياب

## 🎯 الهدف
شرح كيفية رفع تحديث جديد للتطبيق بدون مشاكل

---

## 🔢 نظام الإصدارات

### الإصدار الحالي: `1.1.0`

**نظام الترقيم:**
```
MAJOR.MINOR.PATCH
  1  .  1  .  0

- MAJOR: تغييرات كبيرة غير متوافقة
- MINOR: إضافات جديدة متوافقة
- PATCH: إصلاحات bugs
```

---

## 📝 خطوات رفع تحديث جديد

### 1. تحديث رقم الإصدار في `app.json`

```json
{
  "expo": {
    "version": "1.2.0",        // ← زيادة الرقم
    "runtimeVersion": "1.2.0", // ← تحديث runtime
    "android": {
      "versionCode": 2         // ← زيادة بمقدار 1
    },
    "ios": {
      "buildNumber": "2"       // ← زيادة بمقدار 1
    }
  }
}
```

### 2. تشغيل التطبيق للتأكد

```bash
# تثبيت dependencies الجديدة
npm install

# تشغيل التطبيق للاختبار
npm start

# للأندرويد
npm run android

# للـ iOS
npm run ios
```

### 3. بناء التطبيق

**للأندرويد (APK):**
```bash
# بناء APK للإنتاج
eas build --platform android --profile production

# أو باستخدام expo
expo build:android
```

**للـ iOS:**
```bash
eas build --platform ios --profile production
```

### 4. رفع التحديث OTA (Over-The-Air)

**ميزة:** المستخدمون يحصلون على التحديث تلقائياً بدون تحميل من المتجر!

```bash
# نشر تحديث OTA
eas update --branch production --message "تحسينات الأداء والواجهة"

# أو باستخدام expo
expo publish
```

---

## ⚡ التحديثات السريعة (OTA)

### متى تستخدم OTA؟
✅ تحديثات JavaScript/React Native
✅ تغييرات الواجهة
✅ إصلاحات bugs بسيطة
✅ تحديثات المحتوى

### متى تحتاج Build جديد؟
❌ تغيير Native Modules
❌ إضافة permissions جديدة
❌ تحديث native dependencies
❌ تغيير app.json (icons, splash, permissions)

---

## 🔍 فحص التحديثات

### في الكود (تلقائي):
التطبيق يفحص التحديثات تلقائياً عند الفتح بفضل:
```json
"updates": {
  "enabled": true,
  "checkAutomatically": "ON_LOAD"
}
```

### يدوياً:
```bash
# فحص آخر تحديث
eas update:list --branch production
```

---

## 📊 مثال عملي

### سيناريو: إصلاح bug بسيط

**قبل:**
```json
"version": "1.1.0",
"versionCode": 1
```

**بعد:**
```json
"version": "1.1.1",      // PATCH +1
"runtimeVersion": "1.1.1",
"versionCode": 2         // +1
```

**الخطوات:**
```bash
# 1. تحديث app.json
# 2. اختبار التطبيق
npm start

# 3. نشر OTA (لا حاجة لـ build جديد!)
eas update --branch production --message "إصلاح مشكلة تسجيل الحضور"
```

---

## 🎨 سيناريو: إضافة ميزة جديدة

**قبل:**
```json
"version": "1.1.0",
"versionCode": 1
```

**بعد:**
```json
"version": "1.2.0",      // MINOR +1
"runtimeVersion": "1.2.0",
"versionCode": 2
```

**الخطوات:**
```bash
# 1. تطوير الميزة
# 2. تحديث app.json
# 3. اختبار شامل
npm start

# 4. نشر OTA
eas update --branch production --message "إضافة ميزة تصدير Excel"
```

---

## 🚨 حل المشاكل الشائعة

### المشكلة: التحديث لا يظهر للمستخدمين

**الحل:**
1. تأكد من تحديث `runtimeVersion`
2. تأكد من `updates.enabled: true`
3. أغلق التطبيق تماماً وافتحه من جديد (force close)
4. تحقق من الإنترنت

### المشكلة: Build فشل

**الحل:**
```bash
# مسح cache
npm cache clean --force
rm -rf node_modules
npm install

# إعادة المحاولة
eas build --platform android --clear-cache
```

### المشكلة: تعارض versions

**الحل:**
تأكد من:
- `version` = `runtimeVersion`
- `versionCode` أكبر من السابق
- لا يوجد أخطاء في `app.json`

---

## ✅ Checklist قبل النشر

```markdown
- [ ] تحديث version في app.json
- [ ] تحديث runtimeVersion
- [ ] زيادة versionCode (Android)
- [ ] زيادة buildNumber (iOS)
- [ ] اختبار التطبيق محلياً
- [ ] فحص الأخطاء (linting)
- [ ] git commit & push
- [ ] نشر OTA أو Build
- [ ] اختبار التحديث على جهاز حقيقي
```

---

## 🎯 Best Practices

### DO ✅
- زيادة version بشكل منطقي
- كتابة رسالة واضحة للتحديث
- اختبار قبل النشر
- استخدام OTA للتحديثات السريعة
- الاحتفاظ بـ changelog

### DON'T ❌
- تخطي versions
- نشر بدون اختبار
- نسيان تحديث versionCode
- تغيير runtimeVersion بدون داعٍ
- build جديد لكل تغيير صغير

---

## 📱 مراقبة التحديثات

### معرفة من حمّل التحديث:
```bash
# عرض إحصائيات التحديثات
eas update:view [update-id]

# عرض قائمة التحديثات
eas update:list --branch production
```

---

## 🔗 روابط مفيدة

- [Expo Updates Documentation](https://docs.expo.dev/versions/latest/sdk/updates/)
- [EAS Build Documentation](https://docs.expo.dev/build/introduction/)
- [Semantic Versioning](https://semver.org/)

---

## 💡 نصائح للإنتاج

1. **استخدم branches:**
   - `development` - للتطوير
   - `staging` - للاختبار
   - `production` - للمستخدمين

2. **Automated Testing:**
   ```bash
   npm test
   npm run lint
   ```

3. **Git Tags:**
   ```bash
   git tag v1.2.0
   git push --tags
   ```

4. **Changelog:**
   احتفظ بملف `CHANGELOG.md` يوثق التغييرات

---

## 🎉 خلاصة

**للتحديثات البسيطة:**
```bash
1. تحديث app.json
2. eas update --branch production
```

**للتحديثات الكبيرة:**
```bash
1. تحديث app.json
2. eas build --platform android
3. رفع للمتجر
```

**سهل وسريع! 🚀**

