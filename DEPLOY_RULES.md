# 🔥 نشر قواعد Firestore

## المشكلة
التطبيق يعرض أخطاء "Missing or insufficient permissions" لأن قواعد Firestore غير منشورة.

## الحل السريع (من Firebase Console)

1. افتح [Firebase Console](https://console.firebase.google.com/)
2. اختر مشروعك: **learnzplus**
3. اذهب إلى **Firestore Database** → **Rules**
4. انسخ محتوى ملف `firestore.rules`
5. الصق في محرر القواعد
6. اضغط **Publish**

## الحل باستخدام Firebase CLI

### 1. تثبيت Firebase CLI (إذا لم يكن مثبتاً)
```bash
npm install -g firebase-tools
```

### 2. تسجيل الدخول
```bash
firebase login
```

### 3. تهيئة المشروع (إذا لم يكن مهيأ)
```bash
firebase init firestore
```
- اختر المشروع: **learnzplus**
- استخدم الملفات الموجودة: `firestore.rules`

### 4. نشر القواعد
```bash
firebase deploy --only firestore:rules
```

## التحقق من النشر

بعد النشر، جرّب تسجيل الدخول في التطبيق مرة أخرى. يجب أن تختفي أخطاء الصلاحيات.

## ملاحظات

- القواعد المحدثة تسمح لأي مستخدم مسجل الدخول بإنشاء/قراءة وثائق المعلمين
- التحديث والحذف مقصوران على مالك الوثيقة فقط
- تأكد من أن المستخدم مسجل الدخول قبل محاولة الوصول إلى Firestore

