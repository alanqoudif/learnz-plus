# تطبيق الحضور والغياب للمعلمين

تطبيق متطور لإدارة حضور وغياب الطلاب باستخدام Supabase كقاعدة بيانات سحابية، مصمم خصيصاً للمعلمين.

## المميزات

### 🔐 نظام تسجيل الدخول
- **تسجيل دخول بالرقم والاسم**: رقم الهاتف فريد لكل معلم
- **إنشاء حساب تلقائي**: للمعلمين الجدد
- **حفظ جلسة المستخدم**: تسجيل دخول تلقائي

### 📚 إدارة الفصول الدراسية
- **إضافة فصول جديدة**: مع الشعب المتعددة
- **حذف الفصول**: مع جميع البيانات المرتبطة
- **عرض جميع الفصول**: للمعلم

### 👥 إدارة الطلاب
- **إضافة الطلاب**: يدوياً للفصول
- **حذف الطلاب**: مع سجلات الحضور
- **عرض قائمة الطلاب**: مع أرقامهم

### 📊 نظام الحضور والغياب
- **تسجيل الحضور**: واجهة تفاعلية مع إيماءات السحب
- **عرض تاريخ الحضور**: مع إحصائيات مفصلة
- **جلسات الحضور**: منظمة حسب التاريخ
- **واجهة عربية**: تصميم باللغة العربية مع دعم RTL

## كيفية الاستخدام

### 1. تسجيل الدخول
- أدخل اسمك ورقم هاتفك
- اضغط على "تسجيل الدخول"

### 2. إضافة فصل دراسي
- اضغط على "إضافة فصل" في الصفحة الرئيسية
- أدخل اسم الفصل (مثل: الخامس، السادس)
- أدخل الشعبة (مثل: أ، ب، ج)
- اضغط على "إضافة الفصل"

### 3. إدارة الطلاب
- اضغط على "إدارة الطلاب" في الفصل المطلوب
- اضغط على "إضافة طالب"
- أدخل اسم الطالب
- كرر العملية لكل طالب

### 4. تسجيل الحضور
- اضغط على "تسجيل الحضور" في الفصل المطلوب
- اضغط على "بدء التسجيل"
- اسحب كارت الطالب:
  - **لليمين** = حاضر
  - **ليسار** = غائب
- أو استخدم الأزرار اليدوية

## التقنيات المستخدمة

- **React Native** - إطار العمل الأساسي
- **Expo** - منصة التطوير والنشر
- **TypeScript** - لغة البرمجة
- **Supabase** - قاعدة البيانات السحابية
- **React Navigation** - التنقل بين الشاشات
- **React Native Gesture Handler** - التعامل مع الإيماءات

## الإعداد السريع

### 1. إعداد Supabase
```bash
# اذهب إلى supabase.com وأنشئ مشروع جديد
# احصل على Project URL و anon key
```

### 2. إنشاء ملف .env
```env
EXPO_PUBLIC_SUPABASE_URL=your_project_url_here
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
EXPO_PUBLIC_OPENAI_API_KEY=your_openai_api_key_here
```

**ملاحظة**: مفتاح OpenAI API مطلوب لاستخدام ميزة استخراج أسماء الطلاب من الصور (OCR). يمكنك الحصول على مفتاح من [OpenAI Platform](https://platform.openai.com/api-keys).

### 3. إنشاء قاعدة البيانات
```sql
-- انسخ والصق محتوى database/schema.sql في SQL Editor
```

### 4. تشغيل التطبيق
```bash
npm install
npm start
```

### المتطلبات
- Node.js (الإصدار 18 أو أحدث)
- Expo CLI
- مشروع Supabase
- تطبيق Expo Go على الهاتف

### أوامر إضافية
```bash
# تشغيل على Android
npm run android

# تشغيل على iOS
npm run ios

# تشغيل على الويب
npm run web
```

## هيكل المشروع

```
src/
├── config/
│   └── supabase.ts          # إعدادات Supabase
├── context/
│   └── AppContext.tsx       # إدارة الحالة العامة
├── screens/
│   ├── LoginScreen.tsx      # شاشة تسجيل الدخول
│   ├── DashboardScreen.tsx  # الشاشة الرئيسية
│   ├── AddClassScreen.tsx   # إضافة فصل جديد
│   ├── StudentManagementScreen.tsx # إدارة الطلاب
│   ├── AttendanceScreen.tsx # تسجيل الحضور
│   └── AttendanceHistoryScreen.tsx # تاريخ الحضور
├── services/
│   └── supabaseService.ts   # خدمات قاعدة البيانات
├── types/
│   └── index.ts            # تعريفات الأنواع
└── utils/
    ├── constants.ts        # الثوابت
    ├── helpers.ts          # الدوال المساعدة
    ├── notifications.ts    # التنبيهات
    ├── theme.ts           # الألوان والخطوط
    └── validation.ts      # التحقق من البيانات
```

## قاعدة البيانات

### الجداول الرئيسية
- **teachers** - بيانات المعلمين
- **classes** - الفصول الدراسية
- **students** - الطلاب
- **attendance_sessions** - جلسات الحضور
- **attendance_records** - سجلات الحضور

### العلاقات
- معلم واحد → عدة فصول
- فصل واحد → عدة طلاب
- فصل واحد → عدة جلسات حضور
- جلسة واحدة → عدة سجلات حضور

## الأمان

- **Row Level Security (RLS)** مفعل على جميع الجداول
- **سياسات أمان** مخصصة للتحكم في الوصول
- **مفاتيح API** محمية ومشفرة
- **التحقق من البيانات** على مستوى التطبيق وقاعدة البيانات

## الأداء

- **فهارس قاعدة البيانات** لتحسين سرعة الاستعلامات
- **تحميل البيانات** عند الحاجة فقط
- **تخزين مؤقت** للبيانات المتكررة
- **تحسين الاستعلامات** لتقليل استهلاك البيانات

## المميزات المستقبلية

- [x] قاعدة بيانات سحابية مع Supabase
- [x] نظام تسجيل دخول متطور
- [x] إحصائيات مفصلة للحضور
- [ ] إضافة الطلاب من الصور (OCR)
- [ ] تصدير تقارير الحضور
- [ ] إشعارات للطلاب الغائبين
- [ ] دعم عدة معلمين
- [ ] تطبيق ويب مصاحب

## المساهمة

نرحب بالمساهمات! يرجى:

1. عمل Fork للمشروع
2. إنشاء فرع جديد للميزة
3. إجراء التغييرات
4. إرسال Pull Request

## الترخيص

هذا المشروع مرخص تحت رخصة MIT.

## الدعم

إذا واجهت أي مشاكل أو لديك اقتراحات، يرجى فتح issue في GitHub.

---

**تم تطويره بـ ❤️ للمعلمين العرب**
